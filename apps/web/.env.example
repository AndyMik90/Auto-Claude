# Auto Claude Web UI Environment Variables
# Copy this file to .env and set your values

# ============================================
# WEBSOCKET SERVER CONNECTION (REQUIRED)
# ============================================

# WebSocket server URL for backend communication
# This connects the web UI to the Python backend WebSocket server
# Development: ws://localhost:8765
# Production: wss://your-domain.com/ws
VITE_WS_URL=ws://localhost:8765

# ============================================
# BACKEND API CONNECTION (OPTIONAL)
# ============================================

# Backend API URL for REST endpoints (if needed)
# This is optional and only used if REST API endpoints are added in the future
# Development: http://localhost:8000
# Production: https://your-domain.com/api
VITE_API_URL=http://localhost:8000

# ============================================
# HOW TO USE
# ============================================

# Option 1: Create a .env file in this directory (apps/web/)
#   Copy this file: cp .env.example .env
#   Then set the values above based on your environment
#
# Option 2: Use default values
#   If no .env file is present, the app will use default development URLs
#
# Note: Vite only exposes variables prefixed with VITE_ to the client
# All VITE_* variables are embedded in the build at compile time

# ============================================
# DEVELOPMENT
# ============================================

# Node environment (automatically set by Vite)
# NODE_ENV=development
