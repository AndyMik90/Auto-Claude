# Comment: 53aea5b723c280f8

**Source:** coderabbitai
**Type:** comment
**File:** `apps/frontend/src/renderer/stores/linear-store.ts`
**Original ID:** 2713898544
**Created:** None
**Severity:** HIGH
**Status:** PENDING

---

## Original Content

_⚠️ Potential issue_ | _🟠 Major_

**Hardcoded error messages violate i18n requirements.**

Per coding guidelines, all user-facing text must use i18n translation keys from `react-i18next`. The error messages here and throughout the async actions use hardcoded strings.

<details>
<summary>Example fix for this function</summary>

```diff
+import { t } from 'i18next';
+
 export async function fetchLinearTickets(filters?: LinearFilters): Promise<void> {
   const store = useLinearStore.getState();
   store.setLoading(true);
   store.setError(null);

   try {
     if (!window.electronAPI?.linear) {
-      console.warn('[fetchLinearTickets] Linear API not available');
+      console.warn(t('linear:errors.apiNotAvailable'));
       return;
     }
     // ...
     if (result.success && result.data) {
       store.setTickets(result.data);
     } else {
-      store.setError(result.error || 'Failed to fetch Linear tickets');
+      store.setError(result.error || t('linear:errors.fetchTicketsFailed'));
     }
   } catch (error) {
-    store.setError(error instanceof Error ? error.message : 'Unknown error');
+    store.setError(error instanceof Error ? error.message : t('common:errors.unknown'));
   }
   // ...
 }
```
</details>

Apply similar i18n changes to other async functions: `validateLinearTicket`, `validateLinearTicketBatch`, `fetchLinearTeams`, `fetchLinearProjects`, `updateLinearTicketWithValidation`, and `createTaskFromLinearTicket`.

<!-- suggestion_start -->

<details>
<summary>📝 Committable suggestion</summary>

> ‼️ **IMPORTANT**
> Carefully review the code before committing. Ensure that it accurately replaces the highlighted code, contains no missing lines, and has no issues with indentation. Thoroughly test & benchmark the code to ensure it meets the requirements.

```suggestion
    if (!window.electronAPI?.linear) {
      console.warn(t('linear:errors.apiNotAvailable'));
      return;
    }

    const result = await window.electronAPI.linear.getLinearIssues(
      filters?.teamId || undefined,
      filters?.projectId || undefined
    );

    if (result.success && result.data) {
      store.setTickets(result.data);
    } else {
      store.setError(result.error || t('linear:errors.fetchTicketsFailed'));
    }
  } catch (error) {
    store.setError(error instanceof Error ? error.message : t('common:errors.unknown'));
  } finally {
    store.setLoading(false);
  }
```

</details>

<!-- suggestion_end -->

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In `@apps/frontend/src/renderer/stores/linear-store.ts` around lines 262 - 281,
The catch and error branches in fetchLinearTickets use hardcoded messages;
replace them with i18n translation keys using react-i18next (e.g., call
t('linear.fetchFailed') or similar) instead of literal strings, and ensure you
import/use the useTranslation hook or pass the t function into this store so
store.setError receives translated text; apply the same change pattern to the
other async functions validateLinearTicket, validateLinearTicketBatch,
fetchLinearTeams, fetchLinearProjects, updateLinearTicketWithValidation, and
createTaskFromLinearTicket so all user-facing error messages use t(...)
translation keys rather than hardcoded strings.
```

</details>

<!-- fingerprinting:phantom:medusa:ocelot -->

<!-- This is an auto-generated comment by CodeRabbit -->

---

## Suggested Fix

```typescript
</details>

<!-- suggestion_end -->

<details>
<summary>🤖 Prompt for AI Agents</summary>
```

---

## Implementation Notes

*Status: PENDING - Not yet verified or implemented*

### Verification Checklist

- [ ] Read file at comment location
- [ ] Verify if issue is already fixed
- [ ] Implement fix if needed
- [ ] Re-verify after implementation

