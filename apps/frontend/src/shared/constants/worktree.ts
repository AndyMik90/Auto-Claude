/**
 * Worktree configuration constants.
 *
 * Centralized constants for worktree path configuration to ensure consistency
 * across frontend components and avoid duplication.
 */

/** Environment variable name for custom worktree base path */
export const WORKTREE_BASE_PATH_VAR = 'WORKTREE_BASE_PATH';

/** Default worktree base path (relative to project root) */
export const DEFAULT_WORKTREE_PATH = '.worktrees';
