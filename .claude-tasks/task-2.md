# Task 2: Feature Components - Part 2

Update GitHubSetupModal.tsx, GitSetupModal.tsx, and CompetitorAnalysisViewer.tsx with Apple HIG styling.

## GitHubSetupModal.tsx
- Replace `rounded-lg` with `rounded-xl`
- Add `.tap-target` to buttons
- Add `focus:ring-2 focus:ring-ring/40` to form inputs

## GitSetupModal.tsx
- Replace `rounded-lg` with `rounded-xl`
- Add `.tap-target` to buttons
- Add focus states to form controls

## CompetitorAnalysisViewer.tsx
- Replace `rounded-lg` with `rounded-xl`
- Add `.tap-target` to interactive elements
