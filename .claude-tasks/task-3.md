# Task 3: Animation Patterns

Update PhaseProgressIndicator.tsx, ZenMode.tsx, ProactiveSwapListener.tsx, TerminalGrid.tsx, SortableTaskCard.tsx with Apple HIG animation patterns.

## PhaseProgressIndicator.tsx
- Replace `easeInOut` with `easeOut`
- Update durations to 0.2s-0.3s (Apple scale)

## ZenMode.tsx
- Replace `duration-300` with `duration-200 ease-out`

## ProactiveSwapListener.tsx
- Replace `duration-300` with `duration-200 ease-out`

## TerminalGrid.tsx
- Replace `duration-300` with `duration-200 ease-out`

## SortableTaskCard.tsx
- Replace `duration-200` with `duration-150 ease-out`
