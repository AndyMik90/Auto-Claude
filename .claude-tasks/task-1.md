# Task 1: Feature Components - Part 1

Update WelcomeScreen.tsx, AddFeatureDialog.tsx, and TaskCreationWizard.tsx with Apple HIG styling.

## WelcomeScreen.tsx
- Add `rounded-xl` to card containers
- Add `.tap-target` and `transition-all duration-150 ease-out` to buttons
- Add `transition-all duration-150 ease-out` to hover states
- Use `h-11` height for primary buttons

## AddFeatureDialog.tsx
- Add `.tap-target` to form inputs/buttons
- Add `focus:ring-2 focus:ring-ring/40 focus:ring-offset-2`
- Change `rounded-lg` to `rounded-xl`

## TaskCreationWizard.tsx
- Add `.tap-target` to form elements
- Add `focus:ring-2 focus:ring-ring/40`
- Use `rounded-xl` consistently
